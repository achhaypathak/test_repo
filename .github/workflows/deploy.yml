name: Deploy to Dockerhub

on: 
    push:
        branches: [ "main" ]
    pull_request: 
        branches: [ "main" ]

jobs:
    build-and-push:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v4

            - name: Log in to Dockerhub
              uses: docker/login-action@v3
              with:
                username: ${{ secrets.DOCKER_USERNAME }}
                password: ${{ secrets.DOCKER_PASSWORD }}

            - name: Build and Push Docker image
              uses: docker/build-push-action@v5
              with:
                context: .
                file: ./Dockerfile
                push: true
                tags: achhaypathak/test-image:latest

            - name: Verify Pushed Image
              run: docker pull achhaypathak/test-image:latest

            - name: Deploy using Watchtower
              run: |
                  curl --location 'https://watchtower.dev.gistimpact.org/v1/update' --header 'Authorization: Bearer XMaCNcA4vtnV78mxjOpaZs9EXTUDmKZ8ijj908pxXEtaekb12M' --header 'CF-Access-Client-Id: 16a7b234c06cc0e5b8117ce78e0c0f82.access'  --header 'CF-Access-Client-Secret: 2ac5cab5cdb428f7e2bec1c1628df13f1ad07a2561bf5ddc780eb5c8a7e3b6fe' --ssl-no-revoke

            
              
